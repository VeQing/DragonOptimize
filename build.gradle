buildscript {
    repositories {
        jcenter()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:2.3-SNAPSHOT'
    }
}

plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
}

apply plugin: 'net.minecraftforge.gradle.forge'

version = "1.0.0"
group = "com.dragoncore.optimize"
archivesBaseName = "dragonoptimize"

sourceCompatibility = targetCompatibility = '1.8'
compileJava {
    sourceCompatibility = targetCompatibility = '1.8'
}

minecraft {
    version = "1.12.2-14.23.5.2860"
    runDir = "run"
    mappings = "stable_39"
    makeObfSourceJar = false
}

dependencies {
    // 这里可以添加对龙核 DragonCore 的编译时依赖：
    // deobfProvided 'com.dragoncore:dragoncore:1.12.2-SNAPSHOT'
}

processResources {
    inputs.property "version", project.version
    inputs.property "mcversion", project.minecraft.version

    from(sourceSets.main.resources.srcDirs) {
        include 'mcmod.info'
        expand 'version': project.version, 'mcversion': project.minecraft.version
    }

    from(sourceSets.main.resources.srcDirs) {
        exclude 'mcmod.info'
    }

    // 把 META-INF/MANIFEST.MF 的 FMLCorePlugin 信息写入 jar，
    // 这样 Forge 就会识别为 CoreMod。
    rename 'MANIFEST.MF', 'META-INF/MANIFEST.MF'
}

jar {
    manifest {
        attributes(
                'FMLCorePlugin': 'com.dragoncore.optimize.asm.DragonOptimizeLoadingPlugin',
                'FMLCorePluginContainsFMLMod': 'true',
                'FMLAT': 'dragonoptimize_at.cfg',
                'Implementation-Title': 'DragonOptimize',
                'Implementation-Version': project.version,
                'Implementation-Vendor': 'DragonCore Optimize Team'
        )
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
